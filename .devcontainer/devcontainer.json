{
  "name": "particle-life",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Particle Life Realtime",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "python3 -m venv .venv && . .venv/bin/activate && pip install -U 'pip<25.3' && pip install pip-tools && pip-compile requirements.in && pip install -r requirements.txt && pip install -e ."
}
