[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sunlight"
version = "0.1.0"
description = "Initial Sun/Earth web visualization"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
